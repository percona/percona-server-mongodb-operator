/data/db/logs/*.log {
   daily
   minsize 10M
   maxsize 100M
   rotate 10
   missingok
   nocompress
   notifempty
   sharedscripts
   postrotate
       /bin/kill -SIGUSR1 `cat /var/run/mongodb/mongod.pid`
   endscript
}
